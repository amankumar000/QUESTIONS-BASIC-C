#include<stdio.h>
#include<math.h>
int main()
{
   int x,y,s;
   printf("value of x ");
   scanf("%d",&x);

   printf("value of y ");
   scanf("%d",&y);

   s=pow(x,y);
   printf("power of x %d",s);

   return 0;
}
